define(['controller/_direccionController','delegate/direccionDelegate'], function() {
    App.Controller.DireccionController = App.Controller._DireccionController.extend({

    });
    return App.Controller.DireccionController;
}); 